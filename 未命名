用 React + TypeScript + Tailwind CSS v4 + Lucide React 图标库，做一个完全适配 320x320px 儿童手表的英语学习 APP，名字叫 HelloWorld，面向幼儿园到初中孩子，整体风格 Apple Watch 化、儿童友好、鲜艳柔和、圆角大、按钮好点。

整体硬性要求：
- 屏幕固定 320x320px，手表容器白色、圆角 40px、阴影、内容垂直单列，不能横向滚动。
- 配色：蓝紫、绿、橙红、紫粉、蓝青这几组渐变，全程统一。
- 按钮最小 44x44px，点击有 scale 缩小反馈。
- 字体：标题 16-18px，正文 14px，辅助 12px，中英文混排。
- 数据存在 localStorage：当前等级、是否完成引导。

页面和路由（全部要做，用 react-router data mode）：

1. / 手表主页面（WatchHome）
- 顶部状态栏：WiFi、电池 85%。
- 中间实时时钟（24小时）+ 日期。
- 3x2 APP 图标网格：第一个是 HelloWorld（蓝紫渐变、光圈高亮、可点），剩下 5 个灰色不可点（消息、电话、相机、音乐、运动）。
- 底部小白点指示器 + 提示文字：👆 点击 HelloWorld 开始学习。
- 点 HelloWorld：查 localStorage，有等级就跳 /home，没有跳 /age-selection。

2. /age-selection 年龄段选择页
- 顶部蓝紫渐变标题 HelloWorld、副标题 选择你的年龄段。
- 三个卡片：
  - 幼儿启蒙 PRE-A1：粉红渐变、BookOpen、小学1-2年级
  - 小学进阶 A1：蓝青渐变、GraduationCap、小学3-6年级
  - 初中提升 A2：紫蓝渐变、Award、初中7-9年级
- 点卡片：存 localStorage，跳 /home。

3. /home 主页
- 顶部栏：左边当前等级徽章（可点切换），右边头像按钮（点跳 /profile）。
- 等级菜单：三个按钮，当前等级高亮蓝色。
- 中间标题：开始学习，副标题：选择一个模块开始。
- 三个卡片：
  - 学习助手：绿色、Book、词条查询 → /learning
  - 练习闯关：橙红、Trophy、趣味答题 → /practice
  - 故事解谜：紫粉、Sparkles、冒险探索 → /story
- 底部小提示：每天学习10分钟。

4. /learning 学习助手页
- 顶部绿色渐变：返回、标题、搜索按钮。
- 点搜索显示输入框（带搜索+麦克风图标）。
- 最近学习：3个单词卡片（英文+中文+音量图标）。
- 词条单元：4个单元卡片，显示 Unit 编号、主题、单词数绿色徽章。

5. /practice 练习闯关首页
- 顶部橙红渐变：返回、标题、积分 100。
- 标题：选择难度阶段，副标题：各阶段进度独立，完成闯关拿星星。
- 三个可点卡片：
  - 入门：绿色、🌱、4主题 → /practice/level/beginner
  - 初级：蓝色、🌟、4主题 → /practice/level/intermediate
  - 高级：紫粉、🏆、4主题 → /practice/level/advanced

6. /practice/level/:level 阶段主题列表页
- 顶部按阶段变色：入门绿、初级蓝、高级紫粉。
- 返回、标题：XX阶段。
- 标题：选择主题，副标题：每个主题4个关卡，收集星星。
- 4个主题卡片（全部解锁）：
  - 动物世界：绿色、🐾
  - 缤纷色彩：粉红、🎨
  - 美味水果：橙红、🍎
  - 神奇数字：蓝青、🔢
- 右侧显示 4 颗星星进度 X/4。
- 点卡片跳 /practice/:theme。

7. /practice/:theme 主题闯关路线页（重点）
- 顶部橙红渐变：返回、标题、星星统计 X/4。
- 垂直闯关路线：4个圆形台阶，紫色渐变线连接。
- 每个台阶：
  - 圆环：4段进度（上右下左），完成段青色，拿到星星后绿色，未完成灰色。
  - 中心圆：渐变背景+大星星图标。
  - 星星：拿到就黄色填充放大，否则白色半透明缩小。
  - 解锁规则：第一个台阶直接解锁；后面台阶必须前一个打完守关大将才解锁。
  - 下方显示：主题名、英文名、单元进度 X/4。
- 底部提示框（蓝紫渐变）：完成4个单元后挑战守关大将，C级以上点亮星星。
- 点台阶跳 /practice/:theme/stage/:stageId。

数据结构（用 ts interface）：
Stage：id、themeId、themeName、themeNameEn、color、unitsCompleted(0-4)、totalUnits(4)、bossCompleted、starEarned。

交互与动画：
- 所有按钮 active:scale-95/98。
- 卡片阴影。
- 禁用态：半透明+不可点。
- 圆环进度用 SVG 画，每段90度、间隙4px，颜色动态切换，加过渡动画。
- 实时时钟用 useEffect + setInterval。

输出：一次性生成完整可运行项目，包含所有页面、路由、组件、样式，最后给我可以直接打开的 index.html，能在浏览器跑起来，UI 和逻辑都符合上面描述。
